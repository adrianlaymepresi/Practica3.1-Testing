import { PanelEnConstruccion } from '@/src/components/layout/PanelEnConstruccion';

export default function ClientesPage() {
  return (
    <PanelEnConstruccion
      titulo="Clientes"
      descripcion="Se construira despues de usuarios siguiendo el orden de la base de datos."
    />
  );
}
