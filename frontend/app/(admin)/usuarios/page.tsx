import { PanelEnConstruccion } from '@/src/components/layout/PanelEnConstruccion';

export default function UsuariosPage() {
  return (
    <PanelEnConstruccion
      titulo="Usuarios"
      descripcion="Quedara ligado a empleados y roles con la misma estructura profesional."
    />
  );
}
